// @generated — DO NOT EDIT.
// Regenerate with `node scripts/generate_ugens_rust.mjs`.

use crate::registry::UGenRegistryEntry;
use crate::Rate;

pub(crate) const UGENS: &[UGenRegistryEntry] = &[
    UGenRegistryEntry {
        name: r"COsc",
        rates: &[Rate::Audio, Rate::Control],
        defaults: &[(r"bufnum", None), (r"freq", Some(440.0)), (r"beats", Some(0.5))],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"Chorusing wavetable lookup oscillator. Produces sum of two signals at (freq +/- (beats / 2)). Due to summing, the peak amplitude is twice that of the wavetable."),
        signal_range: None,
        arg_docs: &[(r"beats", r"Beat frequency in Hertz"), (r"bufnum", r"The number of a buffer filled in wavetable format"), (r"freq", r"Frequency in Hertz")],
    },
    UGenRegistryEntry {
        name: r"DegreeToKey",
        rates: &[Rate::Audio, Rate::Control],
        defaults: &[(r"bufnum", None), (r"in", Some(0.0)), (r"octave", Some(12.0))],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"the input signal value is truncated to an integer value and used as an index into an octave repeating table of note values (indices wrap around the table)"),
        signal_range: None,
        arg_docs: &[(r"bufnum", r"Index of the buffer which contains the steps for each scale degree."), (r"in", r"The input signal."), (r"octave", r"The number of steps per octave in the scale. The default is 12.")],
    },
    UGenRegistryEntry {
        name: r"DetectIndex",
        rates: &[Rate::Control, Rate::Scalar],
        defaults: &[(r"bufnum", None), (r"in", Some(0.0))],
        num_outputs: None,
        extends: Some(r"Index"),
        summary: None,
        doc: Some(r"search a buffer for a value"),
        signal_range: None,
        arg_docs: &[],
    },
    UGenRegistryEntry {
        name: r"Formant",
        rates: &[Rate::Audio],
        defaults: &[(r"fundfreq", Some(440.0)), (r"formfreq", Some(1760.0)), (r"bwfreq", Some(880.0))],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"Generates a set of harmonics around a formant frequency at a given fundamental frequency. The frequency inputs are read at control rate only, so if you use an audio rate UGen as an input, it will only be sampled at the start of each audio synthesis block."),
        signal_range: None,
        arg_docs: &[(r"bwfreq", r"Pulse width frequency in Hertz. Controls the bandwidth of the formant (control rate)"), (r"formfreq", r"Formant frequency in Hertz (control rate)"), (r"fundfreq", r"Fundamental frequency in Hertz (control rate)")],
    },
    UGenRegistryEntry {
        name: r"Impulse",
        rates: &[Rate::Audio, Rate::Control],
        defaults: &[(r"freq", Some(440.0)), (r"phase", Some(0.0))],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"non band limited impulse oscillator. Outputs a single 1 every freq cycles per second and 0 the rest of the time."),
        signal_range: None,
        arg_docs: &[(r"freq", r"Frequency in Hertz"), (r"phase", r"Phase offset in cycles ( 0..1 )")],
    },
    UGenRegistryEntry {
        name: r"Index",
        rates: &[Rate::Control, Rate::Scalar],
        defaults: &[(r"bufnum", None), (r"in", Some(0.0))],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"the input signal value is truncated to an integer and used as an index into the table"),
        signal_range: None,
        arg_docs: &[],
    },
    UGenRegistryEntry {
        name: r"IndexInBetween",
        rates: &[Rate::Control, Rate::Scalar],
        defaults: &[(r"bufnum", None), (r"in", Some(0.0))],
        num_outputs: None,
        extends: Some(r"Index"),
        summary: None,
        doc: Some(r"finds the (lowest) point in the buffer at which the input signal lies in-between the two values, and returns the index"),
        signal_range: None,
        arg_docs: &[],
    },
    UGenRegistryEntry {
        name: r"LFCub",
        rates: &[Rate::Audio, Rate::Control],
        defaults: &[(r"freq", Some(440.0)), (r"iphase", Some(0.0))],
        num_outputs: None,
        extends: Some(r"LFSaw"),
        summary: None,
        doc: Some(r"an oscillator outputting a sine like shape made of two cubic pieces"),
        signal_range: None,
        arg_docs: &[(r"freq", r"Frequency in Hertz"), (r"iphase", r"Initial phase offset. For efficiency reasons this is a value ranging from 0 to 2.")],
    },
    UGenRegistryEntry {
        name: r"LFGauss",
        rates: &[Rate::Audio, Rate::Control],
        defaults: &[(r"duration", Some(1.0)), (r"width", Some(0.1)), (r"iphase", Some(0.0)), (r"loop", Some(1.0)), (r"action", Some(0.0))],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"A non-band-limited gaussian function oscillator. Output ranges from minval to 1. LFGauss implements the formula: f(x) = exp(squared(x - iphase) / (-2.0 * squared(width))) where x is to vary in the range -1 to 1 over the period dur. minval is the initial value at -1"),
        signal_range: None,
        arg_docs: &[(r"action", r"Action to be evaluated after cycle completes. Default: NO-ACTION."), (r"duration", r"Duration of one full cycle ( for freq input: dur = 1 / freq )"), (r"iphase", r"Initial offset"), (r"loop", r"If loop is > 0, UGen oscillates. Otherwise it calls the done action after one cycle"), (r"width", r"Relative width of the bell. Best to keep below 0.25 when used as envelope.")],
    },
    UGenRegistryEntry {
        name: r"LFPar",
        rates: &[Rate::Audio, Rate::Control],
        defaults: &[(r"freq", Some(440.0)), (r"iphase", Some(0.0))],
        num_outputs: None,
        extends: Some(r"LFSaw"),
        summary: None,
        doc: Some(r"a non band-limited parabolic oscillator outputing a high of 1 and a low of zero."),
        signal_range: None,
        arg_docs: &[(r"freq", r"Frequency in Hertz"), (r"iphase", r"Initial phase offset. For efficiency reasons this is a value ranging from 0 to 2.")],
    },
    UGenRegistryEntry {
        name: r"LFPulse",
        rates: &[Rate::Audio, Rate::Control],
        defaults: &[(r"freq", Some(440.0)), (r"iphase", Some(0.0)), (r"width", Some(0.5))],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"A non-band-limited pulse oscillator. Outputs a high value of one and a low value of zero."),
        signal_range: None,
        arg_docs: &[(r"freq", r"Frequency in Hertz"), (r"iphase", r"Initial phase offset in cycles ( 0..1 )"), (r"width", r"Pulse width duty cycle from zero to one")],
    },
    UGenRegistryEntry {
        name: r"LFSaw",
        rates: &[Rate::Audio, Rate::Control],
        defaults: &[(r"freq", Some(440.0)), (r"iphase", Some(0.0))],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"low freq (i.e. not band limited) sawtooth oscillator"),
        signal_range: None,
        arg_docs: &[(r"freq", r"Frequency in Hertz"), (r"iphase", r"Initial phase offset. For efficiency reasons this is a value ranging from 0 to 2.")],
    },
    UGenRegistryEntry {
        name: r"LFTri",
        rates: &[Rate::Audio, Rate::Control],
        defaults: &[(r"freq", Some(440.0)), (r"iphase", Some(0.0))],
        num_outputs: None,
        extends: Some(r"LFSaw"),
        summary: Some(r"a non-band-limited triangle oscillator"),
        doc: Some(r"The triangle wave shape features two linear slopes and is not as harmonically rich as a sawtooth wave since it only contains odd harmonics (partials). Ideally, this type of wave form is mixed with a sine, square or pulse wave to add a sparkling or bright effect to a sound and is often employed on pads to give them a glittery feel."),
        signal_range: None,
        arg_docs: &[(r"freq", r"Frequency in Hertz"), (r"iphase", r"Initial phase offset. For efficiency reasons this is a value ranging from 0 to 2.")],
    },
    UGenRegistryEntry {
        name: r"Osc",
        rates: &[Rate::Audio, Rate::Control],
        defaults: &[(r"buffer", None), (r"freq", Some(440.0)), (r"phase", Some(0.0))],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"Linear interpolating wavetable lookup oscillator with frequency and phase modulation inputs. This oscillator requires a buffer to be filled with a wavetable format signal. This preprocesses the Signal into a form which can be used efficiently by the Oscillator. The buffer size must be a power of 2. This can be achieved by creating a Buffer object and sending it one of the b_gen messages (sine1, sine2, sine3) with the wavetable flag set to true. This can also be achieved by creating a Signal object and sending it the 'asWavetable' message, saving it to disk, and having the server load it from there."),
        signal_range: None,
        arg_docs: &[(r"buffer", r"Lookup buffer"), (r"freq", r"Frequency in Hertz"), (r"phase", r"Phase offset or modulator in radians")],
    },
    UGenRegistryEntry {
        name: r"Select",
        rates: &[Rate::Audio, Rate::Control],
        defaults: &[(r"which", None), (r"channelsArray", None)],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"select the output signal from an array of inputs"),
        signal_range: None,
        arg_docs: &[(r"channelsArray", r"List of ugens to choose from"), (r"which", r"Index of array to select")],
    },
    UGenRegistryEntry {
        name: r"Shaper",
        rates: &[Rate::Control, Rate::Scalar],
        defaults: &[(r"bufnum", None), (r"in", Some(0.0))],
        num_outputs: None,
        extends: Some(r"Index"),
        summary: None,
        doc: Some(r"performs waveshaping on the input signal by indexing into a table"),
        signal_range: None,
        arg_docs: &[],
    },
    UGenRegistryEntry {
        name: r"SinOsc",
        rates: &[Rate::Audio, Rate::Control],
        defaults: &[(r"freq", Some(440.0)), (r"phase", Some(0.0))],
        num_outputs: None,
        extends: None,
        summary: Some(r"Sine table lookup oscillator"),
        doc: Some(r"Outputs a sine wave with values oscillating between -1 and 1 similar to osc except that the table has already been fixed as a sine table of 8192 entries. Sine waves are often used for creating sub-basses or are mixed with other waveforms to add extra body or bottom end to a sound. They contain no harmonics and consist entirely of the fundamental frequency. This means that they're not suitable for subtractive synthesis i.e. passing through filters such as a hpf or lpf. However, they are useful for additive synthesis i.e. adding multiple sine waves together at different frequencies, amplitudes and phase to create new timbres."),
        signal_range: None,
        arg_docs: &[(r"freq", r"Frequency in Hertz"), (r"phase", r"Phase offset or modulator in radians")],
    },
    UGenRegistryEntry {
        name: r"SinOscFB",
        rates: &[Rate::Audio, Rate::Control],
        defaults: &[(r"freq", Some(440.0)), (r"feedback", Some(0.0))],
        num_outputs: None,
        extends: None,
        summary: Some(r"Sine oscillator with phase modulation feedback"),
        doc: Some(r"Different feedback values results in a modulation between a sine wave and a sawtooth like wave. Overmodulation causes chaotic oscillation."),
        signal_range: None,
        arg_docs: &[(r"feedback", r"amplitude of phase feedback in radians"), (r"freq", r"Frequency of oscillator")],
    },
    UGenRegistryEntry {
        name: r"SyncSaw",
        rates: &[Rate::Audio, Rate::Control],
        defaults: &[(r"syncFreq", Some(440.0)), (r"sawFreq", Some(440.0))],
        num_outputs: None,
        extends: None,
        summary: Some(r"hard sync sawtooth wave oscillator"),
        doc: Some(r"A sawtooth wave that is hard synched to a fundamental pitch. This produces an effect similar to moving formants or pulse width modulation. The sawtooth oscillator has its phase reset when the sync oscillator completes a cycle. This is not a band limited waveform, so it may alias."),
        signal_range: None,
        arg_docs: &[(r"sawFreq", r"Frequency of the slave synched sawtooth wave. saw-freq should always be greater than sync-freq."), (r"syncFreq", r"Frequency of the fundamental.")],
    },
    UGenRegistryEntry {
        name: r"VOsc",
        rates: &[Rate::Audio, Rate::Control],
        defaults: &[(r"bufpos", None), (r"freq", Some(440.0)), (r"phase", Some(0.0))],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"A wavetable lookup oscillator which can be swept smoothly across wavetables. All the wavetables must be allocated to the same size. Fractional values of table will interpolate between two adjacent tables. This oscillator requires at least two buffers to be filled with a wavetable format signal. This preprocesses the Signal into a form which can be used efficiently by the Oscillator. The buffer size must be a power of 2."),
        signal_range: None,
        arg_docs: &[(r"bufpos", r"Buffer index. Can be swept continuously among adjacent wavetable buffers of the same size."), (r"freq", r"Frequency in Hertz"), (r"phase", r"Phase offset of modulator in radians")],
    },
    UGenRegistryEntry {
        name: r"VOsc3",
        rates: &[Rate::Audio, Rate::Control],
        defaults: &[(r"bufpos", None), (r"freq1", Some(110.0)), (r"freq2", Some(220.0)), (r"freq3", Some(440.0))],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"Three variable wavetable oscillators. A wavetable lookup oscillator which can be swept smoothly across wavetables. All the wavetables must be allocated to the same size. Fractional values of table will interpolate between two adjacent tables. This unit generator contains three oscillators at different frequencies, mixed together. This oscillator requires at least two buffers to be filled with a wavetable format signal. This preprocesses the Signal into a form which can be used efficiently by the Oscillator. The buffer size must be a power of 2."),
        signal_range: None,
        arg_docs: &[(r"bufpos", r"Buffer index. Can be swept continuously among adjacent wavetable buffers of the same size."), (r"freq1", r"Frequency in Hertz of first oscillator"), (r"freq2", r"Frequency in Hertz of second oscillator"), (r"freq3", r"Frequency in Hertz of third oscillator")],
    },
    UGenRegistryEntry {
        name: r"VarSaw",
        rates: &[Rate::Audio, Rate::Control],
        defaults: &[(r"freq", Some(440.0)), (r"iphase", Some(0.0)), (r"width", Some(0.5))],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"a variable duty cycle saw wave oscillator"),
        signal_range: None,
        arg_docs: &[(r"freq", r"Frequency in Hertz"), (r"iphase", r"Initial phase offset in cycles ( 0..1 )"), (r"width", r"Duty cycle from zero to one. (0 = downward sawtooth, 0.5 = triangle, 1 = upward sawtooth)")],
    },
    UGenRegistryEntry {
        name: r"Vibrato",
        rates: &[Rate::Audio, Rate::Control],
        defaults: &[(r"freq", Some(440.0)), (r"rate", Some(6.0)), (r"depth", Some(0.02)), (r"delay", Some(0.0)), (r"onset", Some(0.0)), (r"rateVariation", Some(0.04)), (r"depthVariation", Some(0.1)), (r"iphase", Some(0.0))],
        num_outputs: None,
        extends: None,
        summary: Some(r"Models a slow frequency modulation."),
        doc: Some(r"Vibrato is a slow frequency modulation. Consider the systematic deviation in pitch of a singer around a fundamental frequency, or a violinist whose finger wobbles in position on the fingerboard, slightly tightening and loosening the string to add shimmer to the pitch. There is often also a delay before vibrato is established on a note. This UGen models these processes; by setting more extreme settings, you can get back to the timbres of FM synthesis. You can also add in some noise to the vibrato rate and vibrato size (modulation depth) to make for a more realistic motor pattern. The vibrato output is a waveform based on a squared envelope shape with four stages marking out 0.0 to 1.0, 1.0 to 0.0, 0.0 to -1.0, and -1.0 back to 0.0. Vibrato rate determines how quickly you move through these stages."),
        signal_range: None,
        arg_docs: &[(r"delay", r"Delay before vibrato is established in seconds (a singer tends to attack a note and then stabilise with vibrato, for instance)."), (r"depth", r"Size of vibrato frequency deviation around the fundamental, as a proportion of the fundamental. 0.02 = 2% of the fundamental."), (r"depthVariation", r"Noise on the depth of modulation, expressed as a proportion of the depth; can change once per cycle of vibrato. The noise affects independently the up and the down part of vibrato shape within a cycle."), (r"freq", r"Fundamental frequency in Hertz. If the Vibrato UGen is running at audio rate, this must not be a constant, but an actual audio rate UGen"), (r"iphase", r"Initial phase of vibrato modulation, allowing starting above or below the fundamental rather than on it."), (r"onset", r"Transition time in seconds from no vibrato to full vibrato after the initial delay time."), (r"rate", r"Vibrato rate, speed of wobble in Hertz. Note that if this is set to a low value (and definitely with 0.0), you may never get vibrato back, since the rate input is only checked at the end of a cycle."), (r"rateVariation", r"Noise on the rate, expressed as a proportion of the rate; can change once per cycle of vibrato.")],
    },
    UGenRegistryEntry {
        name: r"WrapIndex",
        rates: &[Rate::Control, Rate::Scalar],
        defaults: &[(r"bufnum", None), (r"in", Some(0.0))],
        num_outputs: None,
        extends: Some(r"Index"),
        summary: None,
        doc: Some(r"the input signal value is truncated to an integer value and used as an index into the table (out of range index values are wrapped)"),
        signal_range: None,
        arg_docs: &[],
    },
];
