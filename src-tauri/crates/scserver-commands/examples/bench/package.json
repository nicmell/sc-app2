{
  "name": "scserver-commands-bench",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Browser benchmark: scserver-commands (WASM) vs osc-js for every SC command encode and reply decode.",
  "scripts": {
    "build:wasm": "cd ../.. && cargo component build --release --features component --target wasm32-wasip1 && jco transpile target/wasm32-wasip1/release/scserver_commands.wasm -o examples/bench/pkg",
    "dev": "vite",
    "bench:node": "node --experimental-strip-types run-node.ts",
    "bench:batch": "node --experimental-strip-types run-batch.ts",
    "bench:bundle": "node --experimental-strip-types run-bundle.ts",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "osc-js": "^2.4.1"
  },
  "devDependencies": {
    "@bytecodealliance/jco": "^1.18",
    "@bytecodealliance/preview2-shim": "^0.19",
    "typescript": "^5",
    "vite": "^5"
  }
}
