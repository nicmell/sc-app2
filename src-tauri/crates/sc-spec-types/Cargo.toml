[package]
name = "sc-spec-types"
version = "0.0.1"
edition = "2021"
description = "Serde types for the committed spec JSON files under assets/specs/ — shared by the sc-crates' build scripts."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
