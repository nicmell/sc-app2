// `*.css` (and the other extensions vite-plugin-lit-css handles) resolve to a Lit
// `CSSResult`. Reference the ambient the plugin ships rather than hand-maintaining a copy.
/// <reference types="vite-plugin-lit-css/client" />
