// @generated — DO NOT EDIT.
// Regenerate with `node scripts/generate_ugens_rust.mjs`.

use crate::registry::UGenRegistryEntry;
use crate::Rate;

pub(crate) const UGENS: &[UGenRegistryEntry] = &[
    UGenRegistryEntry {
        name: r"BeatTrack",
        rates: &[Rate::Control],
        defaults: &[(r"chain", None), (r"lock", Some(0.0))],
        num_outputs: Some(4),
        extends: None,
        summary: Some(r#"Autocorrelation based beat tracker" , :rates #{:kr} :num-outs 4 :doc "The underlying model assumes 4/4, but it should work on any isochronous beat structure, though there are biases to 100-120 bpm; a fast 7/8 may not be tracked in that sense. There are four k-rate outputs, being ticks at quarter, eighth and sixteenth level from the determined beat, and the current detected tempo. Note that the sixteenth note output won't necessarily make much sense if the music being tracked has swing; it is provided just as a convenience. This beat tracker determines the beat, biased to the midtempo range by weighting functions. It does not determine the measure level, only a tactus. It is also slow reacting, using a 6 second temporal window for its autocorrelation maneouvres. Don't expect human musician level predictive tracking. On the other hand, it is tireless, relatively general (though obviously best at transient 4/4 heavy material without much expressive tempo variation), and can form the basis of computer processing that is decidedly faster than human."#),
        doc: Some(r"The underlying model assumes 4/4, but it should work on any isochronous beat structure, though there are biases to 100-120 bpm; a fast 7/8 may not be tracked in that sense. There are four k-rate outputs, being ticks at quarter, eighth and sixteenth level from the determined beat, and the current detected tempo. Note that the sixteenth note output won't necessarily make much sense if the music being tracked has swing; it is provided just as a convenience. This beat tracker determines the beat, biased to the midtempo range by weighting functions. It does not determine the measure level, only a tactus. It is also slow reacting, using a 6 second temporal window for its autocorrelation maneouvres. Don't expect human musician level predictive tracking. On the other hand, it is tireless, relatively general (though obviously best at transient 4/4 heavy material without much expressive tempo variation), and can form the basis of computer processing that is decidedly faster than human."),
        signal_range: None,
        arg_docs: &[(r"chain", r"Audio input to track, already passed through an FFT UGen; the expected size of FFT is 1024 for 44100 and 48000 sampling rate, and 2048 for double those. No other sampling rates are supported."), (r"lock", r"If this argument is greater than 0.5, the tracker will lock at its current periodicity and continue from the current phase. Whilst it updates the model's phase and period, this is not reflected in the output until lock goes back below 0.5.")],
    },
    UGenRegistryEntry {
        name: r"BeatTrack2",
        rates: &[Rate::Control],
        defaults: &[(r"busindex", None), (r"numfeatures", None), (r"windowsize", Some(2.0)), (r"phaseaccuracy", Some(0.02)), (r"lock", Some(0.0)), (r"weightingscheme", Some(-2.1))],
        num_outputs: Some(6),
        extends: None,
        summary: None,
        doc: Some(r"Template matching beat tracker. This beat tracker is based on exhaustively testing particular template patterns against feature streams; the testing takes place every 0.5 seconds. The two basic templates are a straight (groove=0) and a swung triplet (groove=1) pattern of 16th notes; this pattern is tried out at scalings corresponding to the tempi from 60 to 180 bpm. This is the cross-corellation method of beat tracking. A majority vote is taken on the best tempo detected, but this must be confirmed by a consistency check after a phase estimate. Such a consistency check helps to avoid wild fluctuating estimates, but is at the expense of an additional half second delay. The latency of the beat tracker with default settings is thus at least 2.5 seconds; because of block-based amortisation of calculation, it is actually around 2.8 seconds latency for a 2.0 second temporal window. This beat tracker is designed to be flexible for user needs; you can try out different window sizes, tempo weights and combinations of features. However, there are no guarantees on stability and effectiveness, and you will need to explore such parameters for a particular situation."),
        signal_range: None,
        arg_docs: &[(r"busindex", r"Audio input to track, already analysed into N features, passed in via a control bus number from which to retrieve consecutive streams."), (r"lock", r"If this argument is greater than 0.5, the tracker will lock at its current periodicity and continue from the current phase. Whilst it updates the model's phase and period, this is not reflected in the output until lock goes back below 0.5."), (r"numfeatures", r"How many features (ie how many control buses) are provided"), (r"phaseaccuracy", r"Relates to how many different phases to test. At the default, 50 different phases spaced by phaseaccuracy seconds would be tried out for 60bpm; 16 would be trialed for 180 bpm. Larger phaseaccuracy means more tests and more CPU cost."), (r"weightingscheme", r"Use (-2.5) for flat weighting of tempi, (-1.5) for compensation weighting based on the number of events tested (because different periods allow different numbers of events within the temporal window) or otherwise a bufnum from 0 upwards for passing an array of 120 individual tempo weights; tempi go from 60 to 179 bpm in steps of one bpm, so you must have a buffer of 120 values."), (r"windowsize", r"Size of the temporal window desired (2.0 to 3.0 seconds models the human temporal window). You might use longer values for stability of estimate at the expense of reactiveness.")],
    },
    UGenRegistryEntry {
        name: r"KeyTrack",
        rates: &[Rate::Control],
        defaults: &[(r"chain", None), (r"keydecay", Some(2.0)), (r"chromaleak", Some(0.5))],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"A (12TET major/minor) key tracker based on a pitch class profile of energy across FFT bins and matching this to templates for major and minor scales in all transpositions. It assumes a 440 Hz concert A reference. Output is 0-11 C major to B major, 12-23 C minor to B minor"),
        signal_range: None,
        arg_docs: &[(r"chain", r"Audio input to track. This must have been pre-analysed by a 4096 size FFT."), (r"chromaleak", r"Each frame, the chroma values are set to the previous value multiplied by the chromadecay. 0.0 will start each frame afresh with no memory."), (r"keydecay", r"Number of seconds for the influence of a window on the final key decision to decay by 40dB (to 0.01 its original value)")],
    },
    UGenRegistryEntry {
        name: r"Loudness",
        rates: &[Rate::Control],
        defaults: &[(r"chain", None), (r"smask", Some(0.25)), (r"tmask", Some(1.0))],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"A perceptual loudness function which outputs loudness in sones; this is a variant of an MP3 perceptual model, summing excitation in ERB bands. It models simple spectral and temporal masking, with equal loudness contour correction in ERB bands to obtain phons (relative dB), then a phon to sone transform. The final output is typically in the range of 0 to 64 sones, though higher values can occur with specific synthesised stimuli."),
        signal_range: None,
        arg_docs: &[(r"chain", r"Audio input to track, which has been pre-analysed by the FFT UGen"), (r"smask", r"Spectral masking param: lower bins mask higher bin power within ERB bands, with a power falloff (leaky integration multiplier) of smask per bin"), (r"tmask", r"Temporal masking param: the phon level let through in an ERB band is the maximum of the new measurement, and the previous minus tmask phons")],
    },
    UGenRegistryEntry {
        name: r"MFCC",
        rates: &[Rate::Control],
        defaults: &[(r"chain", None), (r"numcoeff", Some(13.0))],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: None,
        signal_range: None,
        arg_docs: &[(r"chain", r"Audio input to track, which has been pre-analysed by the FFT UGen"), (r"numcoeff", r"Number of coefficients, defaults to 13, maximum of 42")],
    },
    UGenRegistryEntry {
        name: r"Onsets",
        rates: &[Rate::Control],
        defaults: &[(r"chain", None), (r"threshold", Some(0.5)), (r"odftype", Some(3.0)), (r"relaxtime", Some(1.0)), (r"floor", Some(0.1)), (r"mingap", Some(10.0)), (r"medianspan", Some(11.0)), (r"whtype", Some(1.0)), (r"rawodf", Some(0.0))],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"An onset detector for musical audio signals - detects the beginning of notes/drumbeats/etc. Outputs a control-rate trigger signal which is 1 when an onset is detected, and 0 otherwise. For the FFT chain, you should typically use a frame size of 512 or 1024 (at 44.1 kHz sampling rate) and 50% hop size (which is the default setting in SC). For different sampling rates choose an FFT size to cover a similar time-span (around 10 to 20 ms). The onset detection should work well for a general range of monophonic and polyphonic audio signals. The onset detection is purely based on signal analysis and does not make use of any top-down inferences such as tempo."),
        signal_range: None,
        arg_docs: &[(r"chain", r"an FFT chain"), (r"floor", r"is a lower limit, connected to the idea of how quiet the sound is expected to get without becoming indistinguishable from noise. For some cleanly-recorded classical music with wide dynamic variations, I found it helpful to go down as far as 0.000001."), (r"medianspan", r"specifies the size (in FFT frames) of the median window used for smoothing the detection function before triggering."), (r"mingap", r"specifies a minimum gap (in FFT frames) between onset detections, a brute-force way to prevent too many doubled detections."), (r"odftype", r"the function used to analyse the signal. Options: nPOWER, MAGSUM, COMPLEX, RCOMPLEX (default), PHASE, WPHASE and MKL. Default is RCOMPLEX."), (r"relaxtime", r"specifies the time (in seconds) for the normalisation to forget about a recent onset. If you find too much re-triggering (e.g. as a note dies away unevenly) then you might wish to increase this value."), (r"threshold", r"the detection threshold, typically between 0 and 1, although in rare cases you may find values outside this range useful")],
    },
    UGenRegistryEntry {
        name: r"SpecCentroid",
        rates: &[Rate::Control],
        defaults: &[(r"chain", None)],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"Given an FFT chain, this measures the spectral centroid, which is the weighted mean frequency, or the centre of mass of the spectrum. (DC is ignored.) This can be a useful indicator of the perceptual brightness of a signal."),
        signal_range: None,
        arg_docs: &[(r"chain", r"An FFT chain")],
    },
    UGenRegistryEntry {
        name: r"SpecFlatness",
        rates: &[Rate::Control],
        defaults: &[(r"chain", None)],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"Given an FFT chain this calculates the Spectral Flatness measure, defined as a power spectrum's geometric mean divided by its arithmetic mean. This gives a measure which ranges from approx 0 for a pure sinusoid, to approx 1 for white noise. The measure is calculated linearly. For some applications you may wish to convert the value to a decibel scale - an example of such conversion is shown below."),
        signal_range: None,
        arg_docs: &[(r"chain", r"An FFT chain")],
    },
    UGenRegistryEntry {
        name: r"SpecPcile",
        rates: &[Rate::Control],
        defaults: &[(r"chain", None), (r"fraction", Some(0.5)), (r"interpolate", Some(0.0))],
        num_outputs: None,
        extends: None,
        summary: Some(r#"Find a percentile of FFT magnitude spectrum" , :rates #{:kr} :doc "Given an FFT chain this calculates the cumulative distribution of the frequency spectrum, and outputs the frequency value which corresponds to the desired percentile. For example, to find the frequency at which 90% of the spectral energy lies below that frequency, you want the 90-percentile, which means the value of fraction should be 0.9. The 90-percentile or 95-percentile is often used as a measure of spectral roll-off. The optional third argument interpolate specifies whether interpolation should be used to try and make the percentile frequency estimate more accurate, at the cost of a little higher CPU usage. Set it to 1 to enable this."#),
        doc: Some(r"Given an FFT chain this calculates the cumulative distribution of the frequency spectrum, and outputs the frequency value which corresponds to the desired percentile. For example, to find the frequency at which 90% of the spectral energy lies below that frequency, you want the 90-percentile, which means the value of fraction should be 0.9. The 90-percentile or 95-percentile is often used as a measure of spectral roll-off. The optional third argument interpolate specifies whether interpolation should be used to try and make the percentile frequency estimate more accurate, at the cost of a little higher CPU usage. Set it to 1 to enable this."),
        signal_range: None,
        arg_docs: &[(r"chain", r"An FFT chain"), (r"fraction", r"percentage of the spectral energy you which to find the frequency for"), (r"interpolate", r"Interpolation toggle - 0 off 1 on.")],
    },
];
