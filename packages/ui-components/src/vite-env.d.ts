/// <reference types="vite/client" />
// Vite client ambient types for the demo/test pipeline. (The `*.scss` → CSSResult
// declaration lives in css-modules.d.ts; the package no longer uses `?inline`/`?raw`.)
