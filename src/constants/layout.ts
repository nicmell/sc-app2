import type { BoxItem } from "@/types/stores";

export const DEFAULT_LAYOUT: BoxItem[] = [];

export const NUM_ROWS = 8;

export const NUM_COLUMNS = 12;

/** react-grid-layout cell margin, shared with the placeholder pixel math. */
export const MARGIN: [number, number] = [10, 10];
