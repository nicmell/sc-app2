[package]
name = "scserver-commands"
version = "0.0.1"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Typed Rust encoders and parsers for the SuperCollider server command protocol (OSC + NRT score)."
repository = "https://github.com/nicmell/scserver-commands"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
rosc = "0.11"
wit-bindgen-rt = { version = "0.44", features = ["bitflags"], optional = true }

[features]
default = []
component = ["dep:wit-bindgen-rt"]

# NOTE: upstream's [profile.release] (lto, panic=abort, strip — tuned for the
# wasm component build) was dropped here: cargo ignores profiles in non-root
# workspace members. Re-add at the workspace root as a dedicated profile
# if/when the wasm artifacts are built from this workspace.

[package.metadata.component]
package = "scserver:commands"

[package.metadata.component.target]
path = "wit"
world = "scserver"
