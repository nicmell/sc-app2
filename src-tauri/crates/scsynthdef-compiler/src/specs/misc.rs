// @generated — DO NOT EDIT.
// Regenerate with `node scripts/generate_ugens_rust.mjs`.

use crate::registry::UGenRegistryEntry;
use crate::Rate;

pub(crate) const UGENS: &[UGenRegistryEntry] = &[
    UGenRegistryEntry {
        name: r"Ball",
        rates: &[Rate::Audio],
        defaults: &[(r"in", Some(0.0)), (r"g", Some(1.0)), (r"damp", Some(0.0)), (r"friction", Some(0.01))],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"models the path of a bouncing object that is reflected by a vibrating surface"),
        signal_range: None,
        arg_docs: &[(r"damp", r"damping on impact"), (r"friction", r"proximity from which on attraction to surface starts"), (r"g", r"gravity"), (r"in", r"modulated surface level")],
    },
    UGenRegistryEntry {
        name: r"FreeVerb",
        rates: &[Rate::Audio],
        defaults: &[(r"in", None), (r"mix", Some(0.33)), (r"room", Some(0.5)), (r"damp", Some(0.5))],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"A reverb coded from experiments with faust. Valid parameter range from 0 to 1. Values outside this range are clipped by the UGen."),
        signal_range: None,
        arg_docs: &[(r"damp", r"Reverb HF damp. range 0..1"), (r"in", r"The input signal"), (r"mix", r"Dry/wet balance. range 0..1"), (r"room", r"Room size. rage 0..1")],
    },
    UGenRegistryEntry {
        name: r"FreeVerb2",
        rates: &[Rate::Audio],
        defaults: &[(r"in", None), (r"in2", None), (r"mix", Some(0.33)), (r"room", Some(0.5)), (r"damp", Some(0.5))],
        num_outputs: Some(2),
        extends: None,
        summary: None,
        doc: Some(r"A two-channel reverb coded from experiments with faust. Valid parameter range from 0 to 1. Values outside this range are clipped by the UGen."),
        signal_range: None,
        arg_docs: &[(r"damp", r"Reverb HF damp. range 0..1"), (r"in", r"Input signal channel 1"), (r"in2", r"Input signal channel 2"), (r"mix", r"Dry/wet balance. range 0..1"), (r"room", r"Room size. rage 0..1")],
    },
    UGenRegistryEntry {
        name: r"FreqShift",
        rates: &[Rate::Audio],
        defaults: &[(r"in", None), (r"freq", Some(0.0)), (r"phase", Some(0.0))],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"FreqShift implements single sideband amplitude modulation, also known as frequency shifting, but not to be confused with pitch shifting. Frequency shifting moves all the components of a signal by a fixed amount but does not preserve the original harmonic relationships."),
        signal_range: None,
        arg_docs: &[(r"freq", r"Amount of shift in cycles per second"), (r"in", r"The signal to process"), (r"phase", r"Phase of the frequency shift (0 - 2pi)")],
    },
    UGenRegistryEntry {
        name: r"GVerb",
        rates: &[Rate::Audio],
        defaults: &[(r"in", None), (r"roomsize", Some(10.0)), (r"revtime", Some(3.0)), (r"damping", Some(0.5)), (r"inputbw", Some(0.5)), (r"spread", Some(15.0)), (r"drylevel", Some(1.0)), (r"earlyreflevel", Some(0.7)), (r"taillevel", Some(0.5)), (r"maxroomsize", Some(300.0))],
        num_outputs: Some(2),
        extends: None,
        summary: None,
        doc: Some(r#"A two-channel reverb UGen, based on the \"GVerb\" LADSPA effect by Juhana Sadeharju (kouhia at nic.funet.fi). WARNING - in the current version of the server, there are severe noise issues when you attempt to modify the roomsize or set it to a value greater than 40."#),
        signal_range: None,
        arg_docs: &[(r"damping", r"0 to 1, high frequency rolloff, 0 damps the reverb signal completely, 1 not at all"), (r"drylevel", r"amount of dry signal"), (r"earlyreflevel", r"amount of early reflection level"), (r"in", r"mono input"), (r"inputbw", r"0 to 1, same as damping control, but on the input signal"), (r"maxroomsize", r"to set the size of the delay lines."), (r"revtime", r"in seconds"), (r"roomsize", r"in squared meters."), (r"spread", r"a control on the stereo spread and diffusion of the reverb signal"), (r"taillevel", r"amount of tail level")],
    },
    UGenRegistryEntry {
        name: r"Gendy1",
        rates: &[Rate::Audio, Rate::Control],
        defaults: &[(r"ampdist", Some(1.0)), (r"durdist", Some(1.0)), (r"adparam", Some(1.0)), (r"ddparam", Some(1.0)), (r"minfreq", Some(440.0)), (r"maxfreq", Some(660.0)), (r"ampscale", Some(0.5)), (r"durscale", Some(0.5)), (r"initCps", Some(12.0)), (r"knum", Some(12.0))],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"An implementation of the dynamic stochastic synthesis generator conceived by Iannis Xenakis and described in Formalized Music (1992, Stuyvesant, NY: Pendragon Press) chapter 9 (pp 246-254) and chapters 13 and 14 (pp 289-322). The BASIC program in the book was written by Marie-Helene Serra so I think it helpful to credit her too. The program code has been adapted to avoid infinities in the probability distribution functions. The distributions are hard-coded in C but there is an option to have new amplitude or time breakpoints sampled from a continuous controller input."),
        signal_range: None,
        arg_docs: &[(r"adparam", r"A parameter for the shape of the amplitude probability distribution, requires values in the range 0.0001 to 1 (there are safety checks in the code so don't worry too much if you want to modulate!)"), (r"ampdist", r"Choice of probability distribution for the next perturbation of the amplitude of a control point. The distributions are (adapted from the GENDYN program in Formalized Music): 0- LINEAR,1- CAUCHY, 2- LOGIST, 3- HYPERBCOS, 4- ARCSINE, 5- EXPON, 6- SINUS, Where the sinus (Xenakis' name) is in this implementation taken as sampling from a third party oscillator. See example below."), (r"ampscale", r"Normally 0.0 to 1.0, multiplier for the distribution's delta value for amplitude. An ampscale of 1.0 allows the full range of -1 to 1 for a change of amplitude."), (r"ddparam", r"A parameter for the shape of the duration probability distribution, requires values in the range 0.0001 to 1"), (r"durdist", r"Choice of distribution for the perturbation of the current inter control point duration."), (r"durscale", r"Normally 0.0 to 1.0, multiplier for the distribution's delta value for duration. An ampscale of 1.0 allows the full range of -1 to 1 for a change of duration."), (r"initCps", r"Initialise the number of control points in the memory. Xenakis specifies 12. There would be this number of control points per cycle of the oscillator, though the oscillator's period will constantly change due to the duration distribution."), (r"knum", r"Current number of utilised control points, allows modulation."), (r"maxfreq", r"Maximum allowed frequency of oscillation for the Gendy1 oscillator, so gives the smallest period the duration is allowed to take on."), (r"minfreq", r"Minimum allowed frequency of oscillation for the Gendy1 oscillator, so gives the largest period the duration is allowed to take on.")],
    },
    UGenRegistryEntry {
        name: r"Gendy2",
        rates: &[Rate::Audio, Rate::Control],
        defaults: &[(r"ampdist", Some(1.0)), (r"durdist", Some(1.0)), (r"adparam", Some(1.0)), (r"ddparam", Some(1.0)), (r"minfreq", Some(440.0)), (r"maxfreq", Some(660.0)), (r"ampscale", Some(0.5)), (r"durscale", Some(0.5)), (r"initCps", Some(12.0)), (r"knum", Some(12.0)), (r"a", Some(1.17)), (r"c", Some(0.31))],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"See gendy1 help file for background. This variant of GENDYN is closer to that presented in Hoffmann, Peter. (2000) The New GENDYN Program. Computer Music Journal 24:2, pp 31-38."),
        signal_range: None,
        arg_docs: &[(r"a", r"parameter for Lehmer random number generator perturbed by Xenakis as in ((old*a)+c)%1.0"), (r"adparam", r"A parameter for the shape of the amplitude probability distribution, requires values in the range 0.0001 to 1 (there are safety checks in the code so don't worry too much if you want to modulate!)"), (r"ampdist", r"Choice of probability distribution for the next perturbation of the amplitude of a control point. The distributions are (adapted from the GENDYN program in Formalized Music): 0- LINEAR, 1- CAUCHY, 2- LOGIST, 3- HYPERBCOS, 4- ARCSINE, 5- EXPON, 6- SINUS, Where the sinus (Xenakis' name) is in this implementation taken as sampling from a third party oscillator."), (r"ampscale", r"Normally 0.0 to 1.0, multiplier for the distribution's delta value for amplitude. An ampscale of 1.0 allows the full range of -1 to 1 for a change of amplitude."), (r"c", r"parameter for Lehmer random number generator perturbed by Xenakis"), (r"ddparam", r"A parameter for the shape of the duration probability distribution, requires values in the range 0.0001 to 1"), (r"durdist", r"Choice of distribution for the perturbation of the current inter control point duration."), (r"durscale", r"Normally 0.0 to 1.0, multiplier for the distribution's delta value for duration. An ampscale of 1.0 allows the full range of -1 to 1 for a change of duration."), (r"initCps", r"Initialise the number of control points in the memory. Xenakis specifies 12. There would be this number of control points per cycle of the oscillator, though the oscillator's period will constantly change due to the duration distribution."), (r"knum", r"Current number of utilised control points, allows modulation."), (r"maxfreq", r"Maximum allowed frequency of oscillation for the Gendy1 oscillator, so gives the smallest period the duration is allowed to take on."), (r"minfreq", r"Minimum allowed frequency of oscillation for the Gendy1 oscillator, so gives the largest period the duration is allowed to take on.")],
    },
    UGenRegistryEntry {
        name: r"Gendy3",
        rates: &[Rate::Audio, Rate::Control],
        defaults: &[(r"ampdist", Some(1.0)), (r"durdist", Some(1.0)), (r"adparam", Some(1.0)), (r"ddparam", Some(1.0)), (r"freq", Some(440.0)), (r"ampscale", Some(0.5)), (r"durscale", Some(0.5)), (r"initCps", Some(12.0)), (r"knum", Some(12.0))],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"See Gendy1 help file for background. This variant of GENDYN normalises the durations in each period to force oscillation at the desired pitch. The breakpoints still get perturbed as in Gendy1. There is some glitching in the oscillator caused by the stochastic effects: control points as they vary cause big local jumps of amplitude. Put ampscale and durscale low to minimise this. All parameters can be modulated at control rate except for initCPs which is used only at initialisation."),
        signal_range: None,
        arg_docs: &[(r"adparam", r"A parameter for the shape of the amplitude probability distribution, requires values in the range 0.0001 to 1 (there are safety checks in the code so don't worry too much if you want to modulate!)"), (r"ampdist", r"Choice of probability distribution for the next perturbation of the amplitude of a control point. The distributions are (adapted from the GENDYN program in Formalized Music): 0- LINEAR,1- CAUCHY, 2- LOGIST, 3- HYPERBCOS, 4- ARCSINE, 5- EXPON, 6- SINUS, Where the sinus (Xenakis' name) is in this implementation taken as sampling from a third party oscillator."), (r"ampscale", r"Normally 0.0 to 1.0, multiplier for the distribution's delta value for amplitude. An ampscale of 1.0 allows the full range of -1 to 1 for a change of amplitude."), (r"ddparam", r"A parameter for the shape of the duration probability distribution, requires values in the range 0.0001 to 1"), (r"durdist", r"Choice of distribution for the perturbation of the current inter control point duration."), (r"durscale", r"Normally 0.0 to 1.0, multiplier for the distribution's delta value for duration. An ampscale of 1.0 allows the full range of -1 to 1 for a change of duration."), (r"freq", r"Oscillation frquency."), (r"initCps", r"Initialise the number of control points in the memory. Xenakis specifies 12. There would be this number of control points per cycle of the oscillator, though the oscillator's period will constantly change due to the duration distribution."), (r"knum", r"Current number of utilised control points, allows modulation.")],
    },
    UGenRegistryEntry {
        name: r"Hilbert",
        rates: &[Rate::Audio],
        defaults: &[(r"in", None)],
        num_outputs: Some(2),
        extends: None,
        summary: None,
        doc: None,
        signal_range: None,
        arg_docs: &[],
    },
    UGenRegistryEntry {
        name: r"MoogFF",
        rates: &[Rate::Audio, Rate::Control],
        defaults: &[(r"in", Some(0.0)), (r"freq", Some(100.0)), (r"gain", Some(2.0)), (r"reset", Some(0.0))],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"A digital implementation of the Moog VCF (filter)."),
        signal_range: None,
        arg_docs: &[(r"freq", r"The cutoff frequency"), (r"gain", r"The filter resonance gain, between zero and 4"), (r"in", r"The input signal"), (r"reset", r"When greater than zero, this will reset the state of the digital filters at the beginning of a computational block.")],
    },
    UGenRegistryEntry {
        name: r"PartConv",
        rates: &[Rate::Audio],
        defaults: &[(r"in", None), (r"fftsize", None), (r"irbufnum", None)],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"Partitioned convolution. Various additional buffers must be supplied. Mono impulse response only! If inputting multiple channels, you'll need independent PartConvs, one for each channel. But the charm is: impulse response can be as large as you like (CPU load increases with IR size. Various tradeoffs based on fftsize choice, due to rarer but larger FFTs. This plug-in uses amortisation to spread processing and avoid spikes). Normalisation factors difficult to anticipate; convolution piles up multiple copies of the input on top of itself, so can easily overload."),
        signal_range: None,
        arg_docs: &[(r"fftsize", r"Spectral convolution partition size (twice partition size). You must ensure that the blocksize divides the partition size and there are at least two blocks per partition (to allow for amortisation)"), (r"in", r"Processing target."), (r"irbufnum", r"Prepared buffer of spectra for each partition of the inpulse response")],
    },
    UGenRegistryEntry {
        name: r"PitchShift",
        rates: &[Rate::Audio],
        defaults: &[(r"in", None), (r"windowSize", Some(0.2)), (r"pitchRatio", Some(1.0)), (r"pitchDispersion", Some(0.0)), (r"timeDispersion", Some(0.0))],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"A time domain granular pitch shifter. Grains have a triangular amplitude envelope and an overlap of 4:1."),
        signal_range: None,
        arg_docs: &[(r"in", r"The input signal."), (r"pitchDispersion", r"The maximum random deviation of the pitch from the pitchRatio."), (r"pitchRatio", r"The ratio of the pitch shift. Must be from 0.0 to 4.0"), (r"timeDispersion", r"A random offset of from zero to timeDispersion seconds is added to the delay of each grain. Use of some dispersion can alleviate a hard comb filter effect due to uniform grain placement. It can also be an effect in itself. timeDispersion can be no larger than windowSize."), (r"windowSize", r"The size of the grain window in seconds. This value cannot be modulated.")],
    },
    UGenRegistryEntry {
        name: r"Pluck",
        rates: &[Rate::Audio],
        defaults: &[(r"in", Some(0.0)), (r"trig", Some(1.0)), (r"maxdelaytime", Some(0.2)), (r"delaytime", Some(0.2)), (r"decaytime", Some(1.0)), (r"coef", Some(0.5))],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"Implements the Karplus-Strong style of synthesis, where a delay line (normally starting with noise) is filtered and fed back on itself so that over time it becomes periodic."),
        signal_range: None,
        arg_docs: &[(r"coef", r"The coef of the internal OnePole filter. Values should be between -1 and +1 (larger values will be unstable... so be careful!)."), (r"decaytime", r"Time for the echoes to decay by 60 decibels. Negative times emphasize odd partials."), (r"delaytime", r"Delay time in seconds."), (r"in", r"An excitation signal."), (r"maxdelaytime", r"The max delay time in seconds (initializes the internal delay buffer)."), (r"trig", r"Upon a negative to positive transition, the excitation signal will be fed into the delay line.")],
    },
    UGenRegistryEntry {
        name: r"Spring",
        rates: &[Rate::Audio],
        defaults: &[(r"in", Some(0.0)), (r"spring", Some(0.0)), (r"damp", Some(0.0))],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"Physical model of resonating spring"),
        signal_range: None,
        arg_docs: &[(r"damp", r"Damping"), (r"in", r"Modulated input force"), (r"spring", r"Spring constant (incl. mass)")],
    },
    UGenRegistryEntry {
        name: r"TBall",
        rates: &[Rate::Audio],
        defaults: &[(r"in", Some(0.0)), (r"g", Some(10.0)), (r"damp", Some(0.0)), (r"friction", Some(0.01))],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"models the impacts of a bouncing object that is reflected by a vibrating surface"),
        signal_range: None,
        arg_docs: &[(r"damp", r"damping on impact"), (r"friction", r"proximity from which on attraction to surface starts"), (r"g", r"gravity"), (r"in", r"modulated surface level")],
    },
];
