// @generated — DO NOT EDIT.
// Regenerate with `node scripts/generate_ugens_rust.mjs`.

use crate::registry::UGenRegistryEntry;
use crate::Rate;

pub(crate) const UGENS: &[UGenRegistryEntry] = &[
    UGenRegistryEntry {
        name: r"DiskIn",
        rates: &[Rate::Audio],
        defaults: &[(r"numChannels", None), (r"bufnum", None), (r"loop", Some(0.0))],
        num_outputs: None,
        extends: None,
        summary: Some(r"stream audio in from disk file"),
        doc: Some(r"Continuously play a longer soundfile from disk. This requires a buffer to be preloaded with one buffer size of sound. If loop is set to 1, the soundfile will loop."),
        signal_range: None,
        arg_docs: &[(r"bufnum", r"id of buffer"), (r"loop", r"Soundfile will loop if 1 otherwise not."), (r"numChannels", r"Number of channels in the incoming audio.")],
    },
    UGenRegistryEntry {
        name: r"DiskOut",
        rates: &[Rate::Audio],
        defaults: &[(r"bufnum", None), (r"channelsArray", None)],
        num_outputs: Some(1),
        extends: None,
        summary: Some(r"stream audio out to disk file"),
        doc: Some(r"The output of DiskOut is the number of frames written to disk. Note that the number of channels in the buffer and the channelsArray must be the same, otherwise DiskOut will fail silently (and not write anything to your file)."),
        signal_range: None,
        arg_docs: &[(r"bufnum", r"the number of the buffer to write to (prepared with /b-write)"), (r"channelsArray", r"the Array of channels to write to the file.")],
    },
    UGenRegistryEntry {
        name: r"In",
        rates: &[Rate::Audio, Rate::Control],
        defaults: &[(r"bus", Some(0.0)), (r"numChannels", Some(1.0))],
        num_outputs: None,
        extends: None,
        summary: Some(r"Read a signal from a bus."),
        doc: Some(r"in:kr is functionally similar to in-feedback. That is it reads all data on the bus whether it is from the current cycle or not. This allows for it to receive data from later in the node order. in:ar reads only data from the current cycle, and will zero data from earlier cycles (for use within that synth; the data remains on the bus). Because of this and the fact that the various out ugens mix their output with data from the current cycle but overwrite data from an earlier cycle it may be necessary to use a private control bus when this type of feedback is desired."),
        signal_range: None,
        arg_docs: &[(r"bus", r"the index of the bus to read in from"), (r"numChannels", r"the number of channels (i.e. adjacent buses) to read in. The default is 1. You cannot modulate this number by assigning it to an argument in a SynthDef.")],
    },
    UGenRegistryEntry {
        name: r"InFeedback",
        rates: &[Rate::Audio],
        defaults: &[(r"bus", Some(0.0)), (r"numChannels", Some(1.0))],
        num_outputs: None,
        extends: None,
        summary: Some(r"read signal from a bus with a current or one cycle old timestamp"),
        doc: Some(r"When the various output ugens (out, offsetOut, x-out) write data to a bus, they mix it with any data from the current cycle, but overwrite any data from the previous cycle. (replace-out overwrites all data regardless.) Thus depending on node order and what synths are writing to thep bus, the data on a given bus may be from the current cycle or be one cycle old at the time of reading. in:ar checks the timestamp of any data it reads in and zeros any data from the previous cycle (for use within that node; the data remains on the bus). This is fine for audio data, as it avoids feedback, but for control data it is useful to be able to read data from any place in the node order. For this reason in:kr also reads data that is older than the current cycle. In some cases we might also want to read audio from a node later in the current node order. This is the purpose of InFeedback. The delay introduced by this is one block size, which equals about 0.0014 sec at the default block size and sample rate. (See the resonator example below to see the implications of this.) The variably mixing and overwriting behaviour of the output ugens can make order of execution crucial. (No pun intended.) For example with a node order like the following the InFeedback ugen in Synth 2 will only receive data from Synth 1 (-> = write out; <- = read in): Synth 1 -> busA This synth overwrites the output of Synth3 before it reaches Synth 2 Synth 2 (with InFeedback) <- busA Synth 3 -> busA If Synth 1 were moved after Synth 2 then Synth 2's InFeedback would receive a mix of the output from Synth 1 and Synth 3. This would also be true if Synth 2 came after Synth1 and Synth 3. In both cases data from Synth 1 and Synth 3 would have the same time stamp (either current or from the previous cycle), so nothing would be overwritten. Because of this it is often useful to allocate a separate bus for feedback. With the following arrangement Synth 2 will receive data from Synth3 regardless of Synth 1's position in the node order. Synth 1 -> busA Synth 2 (with InFeedback) <- busB Synth 3 -> busB + busA"),
        signal_range: None,
        arg_docs: &[(r"bus", r"the index of the bus to read in from."), (r"numChannels", r"the number of channels (i.e. adjacent buses) to read in. The default is 1. You cannot modulate this number by assigning it to an argument in a SynthDef.")],
    },
    UGenRegistryEntry {
        name: r"InTrig",
        rates: &[Rate::Control],
        defaults: &[(r"bus", Some(0.0)), (r"numChannels", Some(1.0))],
        num_outputs: None,
        extends: None,
        summary: Some(r"generates a trigger any time the bus is set"),
        doc: Some(r#"Any time the bus is 'touched' ie. has its value set (using \"/c_set\" etc.), a single impulse trigger will be generated. Its amplitude is the value that the bus was set to."#),
        signal_range: None,
        arg_docs: &[(r"bus", r"the index of the bus to read in from."), (r"numChannels", r"the number of channels (i.e. adjacent buses) to read in. The default is 1. You cannot modulate this number by assigning it to an argument in a SynthDef.")],
    },
    UGenRegistryEntry {
        name: r"LagIn",
        rates: &[Rate::Control],
        defaults: &[(r"bus", Some(0.0)), (r"numChannels", Some(1.0)), (r"lag", Some(0.1))],
        num_outputs: None,
        extends: None,
        summary: Some(r"Read a control signal from a bus with a lag."),
        doc: Some(r"Please document me"),
        signal_range: None,
        arg_docs: &[(r"bus", r"the index of the bus to read in from"), (r"lag", r"lag factor"), (r"numChannels", r"the number of channels (i.e. adjacent buses) to read in. Not modulatable.")],
    },
    UGenRegistryEntry {
        name: r"LocalIn",
        rates: &[Rate::Audio, Rate::Control],
        defaults: &[(r"numChannels", Some(1.0))],
        num_outputs: None,
        extends: None,
        summary: None,
        doc: Some(r"defines buses that are local to the enclosing synth. These are like the global buses, but are more convenient if you want to implement a self contained effect that uses a feedback processing loop. There can only be one audio rate and one control rate local-in per SynthDef. The audio can be written to the bus using local-out."),
        signal_range: None,
        arg_docs: &[(r"numChannels", r"the number of channels (i.e. adjacent buses) to read in. The default is 1. You cannot modulate this argument.")],
    },
    UGenRegistryEntry {
        name: r"LocalOut",
        rates: &[Rate::Audio, Rate::Control],
        defaults: &[(r"channelsArray", None)],
        num_outputs: Some(0),
        extends: None,
        summary: Some(r"write to buses local to a synth"),
        doc: Some(r"local-out writes to buses that are local to the enclosing synth. The buses should have been defined by a local-in ugen. The channelsArray must be the same number of channels as were declared in the LocalIn. These are like the global buses, but are more convenient if you want to implement a self contained effect that uses a feedback processing loop."),
        signal_range: None,
        arg_docs: &[(r"channelsArray", r"an Array of channels or single output to write out. You cannot change the size of this once a SynthDef has been built.")],
    },
    UGenRegistryEntry {
        name: r"OffsetOut",
        rates: &[Rate::Audio],
        defaults: &[(r"bus", None), (r"channelsArray", None)],
        num_outputs: Some(0),
        extends: Some(r"Out"),
        summary: Some(r"write signal to a bus with sample accurate timing"),
        doc: Some(r"Output signal to a bus, the sample offset within the bus is kept exactly; i.e. if the synth is scheduled to be started part way through a control cycle, offset-out will maintain the correct offset by buffering the output and delaying it until the exact time that the synth was scheduled for. This ugen is used where sample accurate output is needed."),
        signal_range: None,
        arg_docs: &[(r"bus", r"the index of the buss to write to. The lowest index numbers are written to the audio hardware."), (r"channelsArray", r"a list of signals or single output to write out. You cannot change the size of this once a synth has been defined.")],
    },
    UGenRegistryEntry {
        name: r"Out",
        rates: &[Rate::Audio, Rate::Control],
        defaults: &[(r"bus", None), (r"channelsArray", None)],
        num_outputs: Some(0),
        extends: None,
        summary: Some(r"write a signal to a bus, adding to previous contents."),
        doc: Some(r"write a signal to a bus, adding to any existing contents N.B. Out is subject to control rate jitter. Where sample accurate output is needed, use OffsetOut. When using an array of bus indexes, the channel array will just be copied to each bus index in the array. So (out:ar [bus1 bus2] channels-array) will be the same as (+ (out:ar bus1 channelsArray) (out:ar bus2 channelsArray))."),
        signal_range: None,
        arg_docs: &[(r"bus", r"the index of the buss to write to. The lowest index numbers are written to the audio hardware."), (r"channelsArray", r"a list of signals or single output to write out. You cannot change the size of this once a synth has been defined.")],
    },
    UGenRegistryEntry {
        name: r"ReplaceOut",
        rates: &[Rate::Audio, Rate::Control],
        defaults: &[(r"bus", None), (r"channelsArray", None)],
        num_outputs: Some(0),
        extends: Some(r"Out"),
        summary: Some(r"Send signal to a bus, overwriting previous contents."),
        doc: Some(r"Out adds its output to a given bus, making it available to all nodes later in the node tree (See Synth and Order-of-execution for more information). ReplaceOut overwrites those contents. This can make it useful for processing."),
        signal_range: None,
        arg_docs: &[(r"bus", r"the index of the buss to write to. The lowest index numbers are written to the audio hardware."), (r"channelsArray", r"a list of signals or single output to write out. You cannot change the size of this once a synth has been defined.")],
    },
    UGenRegistryEntry {
        name: r"SharedIn",
        rates: &[Rate::Control],
        defaults: &[(r"bus", Some(0.0)), (r"numChannels", Some(1.0))],
        num_outputs: None,
        extends: None,
        summary: Some(r"read from a shared control bus (internal dsp only)"),
        doc: Some(r"Reads from a control bus shared between the internal server and the SC client. Control rate only. Writing to a shared control bus from the client is synchronous. When not using the internal server use node arguments or the set method of Bus (or /c_set in messaging style)."),
        signal_range: None,
        arg_docs: &[(r"bus", r"the index of the shared control bus to read from"), (r"numChannels", r"the number of channels (i.e. adjacent buses) to read in. The default is 1. You cannot modulate this number by assigning it to an argument in a SynthDef.")],
    },
    UGenRegistryEntry {
        name: r"SharedOut",
        rates: &[Rate::Control],
        defaults: &[(r"bus", None), (r"channelsArray", None)],
        num_outputs: Some(0),
        extends: None,
        summary: None,
        doc: Some(r"Reads from a control bus shared between the internal server and the SC client. Control rate only. Reading from a shared control bus on the client is synchronous."),
        signal_range: None,
        arg_docs: &[(r"bus", r"the index of the shared control bus to read from"), (r"channelsArray", r"an Array of channels or single output to write out. You cannot change the size of this once a SynthDef has been built.")],
    },
    UGenRegistryEntry {
        name: r"VDiskIn",
        rates: &[Rate::Audio],
        defaults: &[(r"numChannels", None), (r"bufnum", None), (r"rate", Some(1.0)), (r"loop", Some(0.0)), (r"sendID", Some(0.0))],
        num_outputs: None,
        extends: None,
        summary: Some(r"stream in audio from a file (with variable rate)"),
        doc: Some(r"Continuously play a longer soundfile from disk. This requires a buffer to be preloaded with one buffer size of sound."),
        signal_range: None,
        arg_docs: &[(r"bufnum", r"id of buffer"), (r"loop", r"Soundfile will loop if 1 otherwise not."), (r"numChannels", r"Number of channels in the audio"), (r"rate", r"controls the rate of playback. Values below 4 are probably fine, but the higher the value, the more disk activity there is, and the more likelihood there will be a problem."), (r"sendID", r"send an osc message with this id and the file position each time the buffer is reloaded: ['/diskin', nodeID, sendID, frame]")],
    },
    UGenRegistryEntry {
        name: r"XOut",
        rates: &[Rate::Audio, Rate::Control],
        defaults: &[(r"bus", None), (r"xfade", None), (r"channelsArray", None)],
        num_outputs: Some(0),
        extends: None,
        summary: Some(r"write signal to a bus, crossfading with the existing content"),
        doc: Some(r"xfade is a level for the crossfade between what is on the bus and what you are sending. The algorithm is equivalent to this: bus_signal = (input_signal * xfade) + (bus_signal * (1 - xfade));"),
        signal_range: None,
        arg_docs: &[(r"bus", r"the index, or array of indexes, of buses to write to. The lowest index numbers are written to the audio hardware."), (r"channelsArray", r"an Array of channels or single output to write out. You cannot change the size of this once a SynthDef has been built."), (r"xfade", r"crossfade level.")],
    },
];
